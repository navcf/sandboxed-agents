# Local skills

Each direct subdirectory containing a `SKILL.md` is staged into the sandbox
image by `stage.sh` (via the `./skills-local` line in `skills.txt`). With no
skill subdirectories present, staging prints a warning and moves on.
